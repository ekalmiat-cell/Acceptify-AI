{
  "name": "acceptify-ai",
  "private": true,
  "version": "0.1.0",
  "description": "Acceptify AI — foundation workspace (Next.js frontend + FastAPI backend).",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:frontend": "npm run dev --prefix frontend",
    "setup": "powershell -ExecutionPolicy Bypass -File scripts/setup.ps1"
  }
}
